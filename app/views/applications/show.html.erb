<h3>Application <%= @application.id %></h3>


Name: <%= @application.name%></br>
Address: <%= @application.address%></br>
City: <%= @application.city %></br>
State: <%= @application.state %></br>
Zip: <%= @application.zip %></br>
Phone Number: <%= @application.phone_number %></br>
Description: <%= @application.description %></br>
Pets I'm Interested In Adopting:
<% @application.pets.each do |pet| %>
  <section id="applicationpet-<%= pet.id %>">
  <%= link_to pet.name, "/pets/#{pet.id}" %>
  <%= link_to "Approve Application", "/pets/#{pet.id}/#{@application.id}", method: :patch %>
  </section>
<% end %>
