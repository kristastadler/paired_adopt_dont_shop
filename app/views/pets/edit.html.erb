<h3>Edit <%= link_to @pet.name, "/pets/#{@pet.id}" %>'s Information</h3>

<div>
  <%= render partial: '/shelter_pets/form', locals: {path: "/pets/#{@pet.id}", method: :patch, button_text: 'Submit'} %>
</div>
